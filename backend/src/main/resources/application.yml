spring:
  application:
    name: LeagueOfUnitedMinors
  datasource:
    password:
    url: jdbc:postgresql://localhost:5432/postgres
    username:
  jpa:
    hibernate:
      ddl-auto: none #TODO maybe we can do some interesting auto database handling with this?
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        show_sql: true
server:
  error:
    include-message: always
    include-binding-errors: always

